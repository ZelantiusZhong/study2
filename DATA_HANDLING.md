# 数据与隐私处理

- 被试基本信息、实验分组、行为选择、反应时和问卷回答都属于研究数据。
- 原始主库位于 `data/study2-data.json`；研究者端 CSV 是分析副本，不能替代主库备份。
- 研究者端“完整主库备份”可下载整个 JSON；建议每个实验日结束后保存一份带日期的副本。
- 正式收集数据后，不要通过网盘公开分享 `data/study2-data.json`。
- 研究者端的删除按钮会同时删除该被试的登记信息、会话、刺激映射、全部试次与问卷；该操作不可撤销，删除前请先备份。
- 建议按实验日对 `data` 文件夹做离线备份，并为备份设备启用磁盘加密。
- 分析时建议保留匿名被试编号，单独保存姓名、联系方式与编号的对应关系；导出后尽早删除分析表中不需要的直接身份信息。
- 质量标记不自动排除被试。先按预注册规则运行主分析，再以质量标记进行敏感性分析并报告两者差异。
- `ddm` 导出排除了“本轮不购买”，只服务于 A/B 二选一的 DDM/LBA；三选一行为应使用完整 `purchases` 表及 softmax 模型。
- 正式实验开始前，应再次确认伦理审批、知情同意文本、被试费规则、数据保留期限和访问权限。
